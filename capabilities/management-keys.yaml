naftiko: 1.0.0-alpha2
info:
  label: Deepgram Management API — Keys
  description: 'Deepgram Management API — Keys. 4 operations. Lead operation: Deepgram List project API keys. Self-contained
    Naftiko capability covering one Deepgram business surface.'
  tags:
  - Deepgram
  - Keys
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DEEPGRAM_API_KEY: DEEPGRAM_API_KEY
capability:
  consumes:
  - type: http
    namespace: management-keys
    baseUri: https://api.deepgram.com
    description: Deepgram Management API — Keys business capability. Self-contained, no shared references.
    resources:
    - name: v1-projects-project_id-keys
      path: /v1/projects/{project_id}/keys
      operations:
      - name: listprojectkeys
        method: GET
        description: Deepgram List project API keys
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createprojectkey
        method: POST
        description: Deepgram Create a project API key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: v1-projects-project_id-keys-key_id
      path: /v1/projects/{project_id}/keys/{key_id}
      operations:
      - name: getprojectkey
        method: GET
        description: Deepgram Get a project API key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: deleteprojectkey
        method: DELETE
        description: Deepgram Delete a project API key
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.DEEPGRAM_API_KEY}}'
  exposes:
  - type: rest
    namespace: management-keys-rest
    port: 8080
    description: REST adapter for Deepgram Management API — Keys. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v1/projects/{project-id}/keys
      name: v1-projects-project-id-keys
      description: REST surface for v1-projects-project_id-keys.
      operations:
      - method: GET
        name: listprojectkeys
        description: Deepgram List project API keys
        call: management-keys.listprojectkeys
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createprojectkey
        description: Deepgram Create a project API key
        call: management-keys.createprojectkey
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/projects/{project-id}/keys/{key-id}
      name: v1-projects-project-id-keys-key-id
      description: REST surface for v1-projects-project_id-keys-key_id.
      operations:
      - method: GET
        name: getprojectkey
        description: Deepgram Get a project API key
        call: management-keys.getprojectkey
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteprojectkey
        description: Deepgram Delete a project API key
        call: management-keys.deleteprojectkey
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: management-keys-mcp
    port: 9090
    transport: http
    description: MCP adapter for Deepgram Management API — Keys. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: deepgram-list-project-api-keys
      description: Deepgram List project API keys
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: management-keys.listprojectkeys
      outputParameters:
      - type: object
        mapping: $.
    - name: deepgram-create-project-api-key
      description: Deepgram Create a project API key
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: management-keys.createprojectkey
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: deepgram-get-project-api-key
      description: Deepgram Get a project API key
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: management-keys.getprojectkey
      outputParameters:
      - type: object
        mapping: $.
    - name: deepgram-delete-project-api-key
      description: Deepgram Delete a project API key
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: management-keys.deleteprojectkey
      outputParameters:
      - type: object
        mapping: $.
